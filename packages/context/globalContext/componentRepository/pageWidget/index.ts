import folderPageWidget from './folder'

const installPageWidget = {
  install: function (componentRepository) {
    
    componentRepository.registFolder(folderPageWidget)
  }
}
//
export default installPageWidget
